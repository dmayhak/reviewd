# reviewd per-project config
# Note: repos section is used by review-picker.sh to inject into global config
repos:
  - name: reviewd
    path: .
    provider: bitbucket
    workspace: dmayhak
    repo_slug: reviewd

# Project-specific instructions
# instructions: |
#   Add project-specific AI instructions here.
